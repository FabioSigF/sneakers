import { styled } from "styled-components";
import { height } from "../../globalStyle";

export const Wrapper = styled.div `
  padding-bottom: ${height.section};
  padding-top: ${height.section};
`

export const TextListEmpty = styled.div `
  width: 100%;
  text-align: center;
`